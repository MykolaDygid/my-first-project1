# Створення кортежу
point = (10, 20, 30)
# Розпакування кортежу
x, y, z = point
# Виведення значень
print(x)
print(y)
print(z)
# Спроба змінити елемент кортежу
# point[1] = 25
# TypeError виникає тому, що кортежі (tuple) є незмінними (immutable).
# Це означає, що після створення кортежу його елементи не можна змінювати,
# додавати або видаляти.